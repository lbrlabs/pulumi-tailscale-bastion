{
    "name": "@lbrlabs/pulumi-aws-tailscalebastion",
    "version": "${VERSION}",
    "repository": "https://github.com/lbrlabs/pulumi-aws-tailscalebastion",
    "scripts": {
        "build": "tsc",
        "install": "node scripts/install-pulumi-plugin.js resource aws-tailscale ${VERSION}"
    },
    "dependencies": {
        "@pulumi/aws": "^5.0.0",
        "@pulumi/tailscale": "^0.11.0"
    },
    "devDependencies": {
        "typescript": "^3.7.0"
    },
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    },
    "pulumi": {
        "resource": true,
        "name": "aws-tailscale",
        "server": "github://api.github.com/lbrlabs"
    }
}
